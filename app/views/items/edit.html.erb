<% @page_title = "在庫情報の編集" %>
<h1><%= @page_title %></h1>

<%= form_for @item do |form| %>
  <%= render "form", form: form %>
  <div align="right"><input type="submit" class="btn btn-primary" value="更新"></div>
<% end %>